name: building-blocks-01

on:
    workflow_dispatch

jobs:
    echo_hello:
        runs-on: ubuntu-latest
        steps:
            - name: one_line script
              run: echo Hello, world!
    echo_goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed script
              run: |
                echo I will succeed now
                exit 0
            - name : Say goodbye
              run: echo Goodbye, world!
      
